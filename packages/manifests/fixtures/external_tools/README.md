# External Tool Fixture Manifests

These manifests are additive failure drills for `external_tool` support.

They are intentionally not loaded by the normal manifest loader because they
live outside `packages/manifests/pipelines/`.

Use them for:

- operator troubleshooting drills
- test-fixture references
- validation examples of known failure modes
