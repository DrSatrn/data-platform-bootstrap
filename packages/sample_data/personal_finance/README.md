# Personal Finance Sample Data

This directory contains the sample datasets used by the finance vertical slice.
The data is intentionally small and easy to inspect so the learning focus stays
on system behavior rather than data volume.

It now includes:

- transactions
- account balances
- budget rules

Those three inputs are enough to prove curated cashflow, category spend, budget
variance, quality checks, metrics publication, and saved-dashboard hydration.
