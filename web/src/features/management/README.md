This feature directory is an additive staging area for future management-console
work that is intentionally not wired into the main app yet.

The goal is to let parallel work build:

- command-taxonomy utilities
- operator workbench components
- inventory and attention summaries
- mock control-plane layouts

without colliding with active route, page, or auth wiring elsewhere in the
repo.
