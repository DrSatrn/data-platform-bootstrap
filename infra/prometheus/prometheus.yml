# This Prometheus config scrapes the API runtime first because it is the
# current home of health and orchestration metadata endpoints.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: platform-api
    static_configs:
      - targets:
          - api:8080
