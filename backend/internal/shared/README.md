# Shared Package

This package holds low-level shared primitives used across bounded contexts, such as HTTP responses, identifiers, and small utility types. Keep it narrow to avoid turning it into a dumping ground.
