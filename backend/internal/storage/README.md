# Storage Package

This package will encapsulate local filesystem layout, artifact naming, and any future object-store abstraction. Keeping storage concerns behind a boundary helps prevent path logic from leaking throughout the codebase.
