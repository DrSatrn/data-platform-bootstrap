# Config Package

This package parses runtime configuration from environment variables and provides strongly typed settings to the rest of the backend. Defensive parsing matters here because bad config should fail fast and clearly.
