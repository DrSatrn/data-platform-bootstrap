# DB Package

This package is reserved for PostgreSQL persistence, migration helpers, and repository implementations. Database access will be introduced behind interfaces so control-plane logic remains testable.
