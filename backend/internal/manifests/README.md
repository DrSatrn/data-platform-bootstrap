# Manifests Package

This package parses declarative platform manifests from the repository. The manifest layer is the product's configuration surface and should remain strongly typed and easy to validate.
